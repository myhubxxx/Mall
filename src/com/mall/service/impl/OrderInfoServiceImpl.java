package com.mall.service.impl;

import com.mall.service.OrderInfoService;

public class OrderInfoServiceImpl implements OrderInfoService {
	/**
	 * OrderInfo table operate 
	 * 1 check LoadBalance is true or false
	 * 2.if false: (Add) argument Map not index; however, (Search) argument Map must have index
	 */
	
}
